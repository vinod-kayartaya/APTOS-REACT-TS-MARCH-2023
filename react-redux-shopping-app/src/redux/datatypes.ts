export interface Product {
    id: number,
    category: string,
    name: string,
    brand: string,
    description: string,
    quantity_per_unit: string,
    unit_price: number,
    picture: string,
    discount: number,
}